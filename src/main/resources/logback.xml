<!-- src/main/resources/logback.xml -->
<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <withJansi>true</withJansi>
        <encoder>
            <pattern>%cyan(%d{HH:mm:ss.SSS}) %gray([%thread]) %highlight(%-5level) %magenta(%logger{36}) - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="STDOUT" />
    </root>

    <!-- Ajuste o package para com.estapar.parking (ou o pacote correto) -->
    <logger name="com.estapar.parking" level="DEBUG" additivity="false">
        <appender-ref ref="STDOUT" />
    </logger>

    <logger name="io.micronaut.data" level="TRACE"/>
    <logger name="io.micronaut.http" level="DEBUG"/>
    <logger name="io.micronaut.sql" level="DEBUG"/>
</configuration>